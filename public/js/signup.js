// made a sign up js but saw gan has done this is login, so have commented out if we need later and deleted the script in the login handlebars

//const signupFormHandler = async function(event) {
    //event.preventDefault();
  
    //const firstnameEl = document.querySelector("#first-name-signup");
    //const lastnameEl = document.querySelector("#last-name-signup");
    //const emailEl = document.querySelector("#email-signup");
    //const passwordEl = document.querySelector("#password-signup");
    //fetch("/api/user", {
     // method: "post",
     // body: JSON.stringify({
      //  firstname: firstnameEl.value,
      //  lastname: lastnameEl.value,
      //  email: emailEl.value,
     //   password: passwordEl.value
    //  }),
  //    headers: { "Content-Type": "application/json" }
  //  })
   //   .then(function() {
   //     document.location.replace("/dashboard");
  //    })
  //    .catch(err => console.log(err));
 // };
  
 // document
 //   .querySelector("#signup-form")
 //  .addEventListener("submit", signupFormHandler);